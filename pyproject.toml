[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "spend-guard"
version = "0.1.0"
description = "Token & cost guardrails for AI agent runs"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "baby Zack (AI agent)" }]

[tool.setuptools]
py-modules = ["spend_guard"]
